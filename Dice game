#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main() {
    // Підключаємо генератор випадкових чисел
    srand(time(0));

    // Початкові змінні за завданням
    int points = 100;
    int user_guess = 0;
    int user_bet = 0;

    cout << "==========================\n";
    cout << "       ГРА КІСТКИ\n";
    cout << "==========================\n\n";
    cout << "Ваш баланс: " << points << " очок.\n";

    // Отримуємо дані від гравця
    cout << "Загадайте число від 2 до 12: ";
    cin >> user_guess;

    // TODO: Додати перевірку на правильність вводу (щоб не вводили більше 12 або букви)

    cout << "Зробіть ставку: ";
    cin >> user_bet;

    // TODO: Додати перевірку, чи вистачає очок на балансі

    // Тестовий кидок кубиків
    int dice1 = rand() % 6 + 1;
    int dice2 = rand() % 6 + 1;
    int sum = dice1 + dice2;

    cout << "\n[Тест] Кидаємо кубики...\n";
    cout << "Випало: " << dice1 << " та " << dice2 << " (Сума: " << sum << ")\n";

    // TODO: Написати логіку перевірки виграшу (умови < 7 та > 7)
    // TODO: Зробити множення ставки х4, якщо вгадав точну суму
    // TODO: Зробити графічне відображення граней кубика
    // TODO: Зациклити гру (щоб можна було грати кілька раундів)
    // TODO: Додати запис статистики у файл txt

    cout << "\nПрограма в процесі розробки..." << endl;

    return 0;
}
